{
  "name": "me",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "type-check": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "cf:deploy": "npm run build && npx wrangler pages deploy dist --project-name youwillmiss-dev --branch main",
    "cf:project:create": "npx wrangler pages project create youwillmiss-dev --production-branch main"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/three": "^0.183.1",
    "typescript": "^5.9.3",
    "vite": "^5.4.0"
  }
}
